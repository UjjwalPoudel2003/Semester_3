// Inheritance from Game Tester class
public class FullTimeGameTester extends GameTester {
    // Constructor created
    public FullTimeGameTester(String gameName) {
        super(gameName, true);
    }

    // Calculating Salary
    public double calculateSalary(int numberOfHours) {
        int baseSalary;
        return baseSalary = 3000;
    }
}